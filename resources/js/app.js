// resources/js/app.js

import './bootstrap';
import Alpine from 'alpinejs';

window.Alpine = Alpine;
Alpine.start();

// Chart.js — imported where needed via dynamic import or direct script tags
// Axios is available globally via bootstrap.js (window.axios)
